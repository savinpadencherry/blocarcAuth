part of appointment_view;

class _AppointmentDesktop extends StatelessWidget {
  const _AppointmentDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AppointmentDesktop')),
    );
  }
}
