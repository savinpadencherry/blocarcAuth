part of appointment_view;

class _AppointmentTablet extends StatelessWidget {
  const _AppointmentTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AppointmentTablet')),
    );
  }
}
