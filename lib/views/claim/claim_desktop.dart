part of claim_view;

class _ClaimDesktop extends StatelessWidget {
  const _ClaimDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ClaimDesktop')),
    );
  }
}
