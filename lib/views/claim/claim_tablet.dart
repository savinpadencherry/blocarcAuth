part of claim_view;

class _ClaimTablet extends StatelessWidget {
  const _ClaimTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ClaimTablet')),
    );
  }
}
