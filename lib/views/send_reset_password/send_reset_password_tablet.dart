part of send_reset_password_view;

class _SendResetPasswordTablet extends StatelessWidget {
  const _SendResetPasswordTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SendResetPasswordTablet')),
    );
  }
}
