part of send_reset_password_view;

class _SendResetPasswordDesktop extends StatelessWidget {
  const _SendResetPasswordDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SendResetPasswordDesktop')),
    );
  }
}
