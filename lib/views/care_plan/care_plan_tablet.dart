part of care_plan_view;

class _CarePlanTablet extends StatelessWidget {
  const _CarePlanTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('CarePlanTablet')),
    );
  }
}
