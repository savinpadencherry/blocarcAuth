part of care_plan_view;

class _CarePlanDesktop extends StatelessWidget {
  const _CarePlanDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'CarePlanDesktop',
        ),
      ),
    );
  }
}
