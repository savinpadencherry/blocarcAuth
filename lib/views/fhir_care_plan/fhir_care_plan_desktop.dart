part of fhir_care_plan_view;

class _FhirCarePlanDesktop extends StatelessWidget {
  const _FhirCarePlanDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'FhirCarePlanDesktop',
        ),
      ),
    );
  }
}
