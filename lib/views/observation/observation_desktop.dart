part of observation_view;

class _ObservationDesktop extends StatelessWidget {
  const _ObservationDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ObservationDesktop'),
      ),
    );
  }
}
