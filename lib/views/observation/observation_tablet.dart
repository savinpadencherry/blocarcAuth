part of observation_view;

class _ObservationTablet extends StatelessWidget {
  const _ObservationTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ObservationTablet')),
    );
  }
}
