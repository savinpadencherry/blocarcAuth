part of fhir_appointment_list_view;

class _FhirAppointmentListDesktop extends StatelessWidget {
  const _FhirAppointmentListDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'FhirAppointmentListDesktop',
        ),
      ),
    );
  }
}
