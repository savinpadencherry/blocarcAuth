part of medication_request_view;

class _MedicationRequestDesktop extends StatelessWidget {
  const _MedicationRequestDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MedicationRequestDesktop'),
      ),
    );
  }
}
