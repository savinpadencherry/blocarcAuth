part of medication_request_view;

class _MedicationRequestTablet extends StatelessWidget {
  const _MedicationRequestTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('MedicationRequestTablet')),
    );
  }
}
