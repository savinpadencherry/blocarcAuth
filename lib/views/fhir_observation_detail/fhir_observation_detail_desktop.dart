part of fhir_observation_detail_view;

class _FhirObservationDetailDesktop extends StatelessWidget {
  const _FhirObservationDetailDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FhirObservationDetailDesktop')),
    );
  }
}
