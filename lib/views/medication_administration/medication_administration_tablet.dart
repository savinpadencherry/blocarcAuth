part of medication_administration_view;

class _MedicationAdministrationTablet extends StatelessWidget {
  const _MedicationAdministrationTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'MedicationAdministrationTablet',
        ),
      ),
    );
  }
}
