part of medication_administration_view;

class _MedicationAdministrationDesktop extends StatelessWidget {
  const _MedicationAdministrationDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MedicationAdministrationDesktop'),
      ),
    );
  }
}
