part of patient_form_view;

class _PatientFormTablet extends StatelessWidget {
  const _PatientFormTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PatientFormTablet')),
    );
  }
}
