part of patient_form_view;

class _PatientFormDesktop extends StatelessWidget {
  const _PatientFormDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PatientFormDesktop'),
      ),
    );
  }
}
