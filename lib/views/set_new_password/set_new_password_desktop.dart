part of set_new_password_view;

class _SetNewPasswordDesktop extends StatelessWidget {
  const _SetNewPasswordDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SetNewPasswordDesktop')),
    );
  }
}
