part of set_new_password_view;

class _SetNewPasswordTablet extends StatelessWidget {
  const _SetNewPasswordTablet();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SetNewPasswordTablet')),
    );
  }
}
