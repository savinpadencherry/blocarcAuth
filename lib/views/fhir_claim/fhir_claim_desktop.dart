part of fhir_claim_view;

class _FhirClaimDesktop extends StatelessWidget {
  const _FhirClaimDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FhirClaimDesktop')),
    );
  }
}
