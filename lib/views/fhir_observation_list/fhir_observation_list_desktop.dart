part of fhir_observation_list_view;

class _FhirObservationListDesktop extends StatelessWidget {
  const _FhirObservationListDesktop();

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FhirObservationListDesktop')),
    );
  }
}
